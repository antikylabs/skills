/** Visual-explanation cases for `show-me`. */

import { inSuite } from "../../types.ts";
import { VISUAL_CASES } from "./visuals.ts";

export const SHOW_ME_CASES = inSuite("show-me", [...VISUAL_CASES]);
