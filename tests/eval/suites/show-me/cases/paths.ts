/** Fixture paths and skill name for the show-me suite. */

import { fixture, SKILLS } from "../../types.ts";

export const SKILL = SKILLS.showMe;
export const SESSION_FLOW = fixture("show-me", "session-flow.ts");
export const RUN_STATES = fixture("show-me", "run-states.md");
