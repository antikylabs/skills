/**
 * Widens the uniform buffer alignment so the town demo stops flickering.
 *
 * Added in a hurry before a demo. Nobody has checked whether it is still needed.
 */
export const name = 'legacy-hack';
export async function apply({ replace }) { /* ... */ }
