# Goal: add a --json flag to the inspection CLI

## Intent

The inspection output is human-readable only. Anything consuming it has to parse
columns, which breaks whenever the formatting changes.

## Prerequisites

Nothing. This goal can start as written.
