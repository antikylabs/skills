interface:
  display_name: "anti-slop"
  short_description: "Reject tests that cannot fail, discarded errors, stubs, and claims with no evidence"
  default_prompt: "Use $general-anti-slop structure to check this repository, then $general-anti-slop prose on any document the change touched."
