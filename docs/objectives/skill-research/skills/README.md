# Antiky agent skills

Install the collection with the open Agent Skills CLI:

```bash
npx skills add antikylabs/antiky --list
npx skills add antikylabs/antiky --skill build-antiky-games
```

Each directory is an independently installable skill. Skills stay concise and defer version-sensitive API details to the documentation and installed package types in the project being changed.
